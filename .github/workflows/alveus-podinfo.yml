jobs:
  demo:
    name: demo
    secrets:
      inherit: true
    uses: ./.github/workflows/alveus-podinfo-demo.yml
name: podinfo
"on":
  push:
    branches:
    - main
    paths:
    - .alveus/demo/manifests
  workflow_dispatch: {}
