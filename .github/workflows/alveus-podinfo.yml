name: podinfo
"on":
  workflow_dispatch: {}
  push:
    paths:
    - .alveus/demo/manifests
    branches:
    - main
jobs:
  demo:
    name: demo
    uses: ./.github/workflows/alveus-podinfo-demo.yml
    secrets:
      inherit: true
